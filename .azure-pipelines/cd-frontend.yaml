# ==============================================================================
# PIPELINE CD: FRONTEND ZYVENCORE VALENTINE
# ==============================================================================
trigger:
  branches:
    include:
      - develop
      - staging
      - main
      - fixci # Rama temporal para depuración

pr: none

pool:
  vmImage: 'ubuntu-latest'

variables:
  # Evaluación en la raíz usando SourceBranchName (sin refs/heads/)
  - ${{ if eq(variables['Build.SourceBranchName'], 'develop') }}:
      - group: 'VG-ZV-DEV'
  - ${{ if eq(variables['Build.SourceBranchName'], 'staging') }}:
      - group: 'VG-ZV-STG'
  - ${{ if eq(variables['Build.SourceBranchName'], 'main') }}:
      - group: 'VG-ZV-MAIN'

  # Trampa temporal: Fuerza el grupo de variables de DEV para la rama de pruebas
  - ${{ if eq(variables['Build.SourceBranchName'], 'fixci') }}:
      - group: 'VG-ZV-DEV'

stages:
  - stage: Deploy_Web
    displayName: 'Despliegue a Azure Static Web Apps'
    jobs:
      - job: Compilar_y_Desplegar
        steps:
          - task: NodeTool@0
            inputs:
              versionSpec: '24.x'
            displayName: 'Instalar Node.js 24'

          - script: npm ci
            displayName: 'Instalar dependencias exactas'

          - script: npm run test
            displayName: 'Puerta previa: pruebas unitarias (Vitest)'

          - script: npm run build
            displayName: 'Compilar el bundle de producción'
            env:
              VITE_API_BASE_URL: $(DEPLOYED_BACKEND)

          - task: AzureStaticWebApp@0
            displayName: 'CD: Publicar en Azure SWA'
            inputs:
              app_location: '/'
              output_location: 'dist'
              skip_app_build: true
              skip_api_build: true
              azure_static_web_apps_api_token: $(SWA_DEPLOYMENT_TOKEN)